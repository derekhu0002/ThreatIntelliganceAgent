---
description: Initialize the Argo codebase for development.
agent: Init
---

Run the `argo_init` command to set up the Argo codebase for development. This will install dependencies, set up the environment, and prepare the codebase for further work.